import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: "100%",
        alignItems: "flex-start"
    },
    label: {
        fontWeight: "bold",
        color: "#3A506B",
        marginBottom: 5
    },
    input: {
        color: "#000",
        paddingVertical: 10, 
        marginBottom: 10,
        borderWidth: 1,
        width: "100%",
        borderRadius: 4,
        borderColor: "#3A506B",
        paddingLeft: 10
    }
})